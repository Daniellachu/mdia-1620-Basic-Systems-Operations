// example 1 increments

var num = 0;
num = num+1;//can increase more than once
console.log(num);

num +=1; can //can increase moer than once
console.log(num);

num ++; //will always increase by 1 
console.log(num);

console.log("--");

//decrementing

var k = 10;

k = k-1;  //can change the number
console.log(k);

k -=1; //can change the number
console.log(k);

k --; ///will always decrement by one
console.log(k);
