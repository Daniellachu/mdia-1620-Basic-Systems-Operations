// emxample 1
var x = 1;
var y = 2;
console.log( x > 2 && y < 3 ); 

// if one side is wrong but the other one is right -> they are both false ( have to be the same)

console.log(x > 0 && x < 3);
// ifboth sides are true then the entire thing is true

console.log (!(x > 2) && y < 3);